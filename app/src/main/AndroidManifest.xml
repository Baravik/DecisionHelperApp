<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.OpenU.decisionhelperapp">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.DecisionHelperApp"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false">

        <!-- Splash Activity -->
        <activity android:name="com.decisionhelperapp.activities.SplashActivity"
            android:theme="@style/SplashTheme"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity (launcher intent removed) -->
        <activity
            android:name="com.decisionhelperapp.activities.MainActivity"
            android:exported="true">
        </activity>

        <!-- Quiz Activity -->
        <activity
            android:name="com.decisionhelperapp.activities.QuizActivity"
            android:label="@string/quiz_title" />

        <!-- Rating Activity -->
        <activity
            android:name="com.decisionhelperapp.activities.RatingActivity"
            android:label="@string/rating_title" />

        <!-- Admin Panel Activity -->
        <activity
            android:name="com.decisionhelperapp.activities.AdminActivity"
            android:label="@string/admin_title" />
            
        <!-- Scores Activity -->
        <activity
            android:name="com.decisionhelperapp.activities.ScoresActivity"
            android:label="Scores" />

        <!-- Users Activity -->
        <activity
            android:name="com.decisionhelperapp.activities.UsersActivity"
            android:label="Users" />

        <!-- Login Activity -->
        <activity
            android:name="com.decisionhelperapp.activities.LoginActivity"
            android:label="Login" />
        <!-- File Provider (optional, add if you share files) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

    </application>
</manifest>